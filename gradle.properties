# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# Enabled caching to speed up ndk-build rebuilds
org.gradle.caching=true

# 1.3.0: release signing credentials (repo is private, keystore ships in it).
# Used by app/build.gradle signingConfigs.release - locally and in CI.
IKURA_STORE_FILE=vile-viende.keystore
IKURA_STORE_PASSWORD=viende2026
IKURA_KEY_ALIAS=vile
IKURA_KEY_PASSWORD=viende2026
